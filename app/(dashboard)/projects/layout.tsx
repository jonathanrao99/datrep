export const metadata = {
  title: 'My Projects',
  description: 'View and manage your uploaded datasets and their analysis status.',
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
